# JobRadar

JobRadar - простой Telegram-бот для поиска вакансий, которые могут стать входом в IT. Он ищет вакансии через HTML-страницы hh.ru, отсекает колл-центры, продажи и горячие линии, оценивает IT-релевантность и присылает только подходящие варианты.

Проект сделан как практичный MVP: бесплатно, без платных API, без нейросетей и без тяжёлых фреймворков. Для HH используется HTML-парсинг публичных страниц и личного раздела через сессионные cookie.

## Для чего проект

JobRadar помогает найти работу за компьютером с реальными техническими задачами: QA, helpdesk, service desk, поддержка IT-продукта, CRM, интеграции, автоматизация, junior Python, мониторинг и смежные направления.

Главный принцип: лучше 5 нормальных IT-вакансий, чем 100 вакансий из колл-центров.

## Установка

Нужен Python 3.11 или новее.

```bash
pip install -r requirements.txt
```

## Как создать Telegram-бота

1. Откройте Telegram и найдите `@BotFather`.
2. Отправьте команду `/newbot`.
3. Задайте название и username бота.
4. Скопируйте токен, который выдаст BotFather.
5. Узнайте свой Telegram ID, например через бота `@userinfobot`.

## Настройка `.env`

Скопируйте пример:

```bash
cp .env.example .env
```

На Windows можно просто создать рядом файл `.env` и перенести туда значения из `.env.example`.

Заполните:

```env
TELEGRAM_BOT_TOKEN=токен_от_BotFather
TELEGRAM_USER_ID=ваш_telegram_id
HH_AREA=113
HH_HOST=hh.ru
HH_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36"
HH_SESSION_COOKIE=
HH_AUTO_WORKER_SECRET=
APP_BASE_URL=https://job-radar-ashen.vercel.app
WEB_SERVER_HOST=0.0.0.0
WEB_SERVER_PORT=8080
HH_PROXY=
HH_PROXIES=
HH_PROXY_FILE=good_proxies.txt
TELEGRAM_PROXY=
SEARCH_INTERVAL_MINUTES=180
MIN_SCORE_TO_SEND=60
DATABASE_PATH=data/jobradar.db
```

`HH_AREA=113` означает Россию. Регион можно поменять на другой ID из справочника HH.ru. `HH_HOST=hh.ru` оставлен как базовый домен HH.

`HH_SESSION_COOKIE` нужен для личных страниц HH: списка резюме и авторизованных действий. Значение берётся из браузера после входа на hh.ru. `HH_AUTO_WORKER_SECRET` защищает cron endpoint. Реальные cookie и секреты нельзя коммитить.

Если HH.ru возвращает 403 из-за зарубежного маршрута, задайте для HH отдельный российский HTTP-прокси:

```env
HH_PROXY=http://login:password@ru-proxy.example:8080
```

Можно указать несколько прокси через запятую в `HH_PROXIES` или положить список в файл `good_proxies.txt`. Бот будет пробовать маршруты по очереди и перестанет использовать те, на которых HH вернул 403. `TELEGRAM_PROXY` настраивает только Telegram и не влияет на запросы к HH.

Порядок маршрутов задаётся через `HH_PROXY_MODE`: `direct_only`, `proxy_only`, `proxy_then_direct` или `direct_then_proxy`. По умолчанию используется `direct_then_proxy`.

Проверить список прокси можно командой:

```bash
python proxy_checker.py
```

Проверить HTML-доступ, User-Agent и cookie можно через команду бота `/hh_diag`.

## Веб-страница проекта

В проекте осталась отдельная страница в папке `web`. Её можно использовать как публичное описание JobRadar, но для подключения HH она больше не нужна.

Локальный запуск:

```bash
cd web
npm install
npm run dev
```

Проверка сборки:

```bash
npm run build
```

Для Vercel укажите `Root Directory = web`.

## Запуск

```bash
python main.py
```

После запуска бот отвечает на команды, поднимает web endpoints для cron на `WEB_SERVER_PORT` и раз в несколько часов сам проверяет новые вакансии.

## Команды бота

- `/start` - приветствие и краткое описание.
- `/search` - ручной поиск свежих вакансий.
- `/settings` - настройки поиска, режимов и лимитов.
- `/hh` - подключение HH и выбор резюме.
- `/stats` - статистика базы, очереди и откликов.
- `/applications` - журнал откликов и очередь.
- `/stop_auto` - экстренно выключить автоотклики.
- `/help` - подсказка по командам.

## Подключение HH по cookie

1. Запустите `python main.py`.
2. Войдите на hh.ru в обычном браузере под нужным аккаунтом.
3. Скопируйте заголовок `Cookie` для запроса к `https://hh.ru/applicant/resumes`.
4. Вставьте значение в `.env` как `HH_SESSION_COOKIE=...` и перезапустите JobRadar.
5. Откройте в Telegram раздел «🔗 HH подключение», нажмите «Проверить подключение», затем выберите резюме.

При каждом запуске JobRadar проверяет `HH_SESSION_COOKIE`. Если HH не принимает cookie, бот пишет владельцу напоминание и временно отключает авторизованные проверки. Поиск вакансий продолжит работать по публичным страницам, но резюме и распознавание внешних откликов будут недоступны до обновления cookie.

Быстрый порядок обновления cookie:

1. Откройте hh.ru в браузере и войдите в нужный аккаунт.
2. Откройте `https://hh.ru/applicant/resumes`.
3. В DevTools → Network выберите запрос к `/applicant/resumes`.
4. Скопируйте весь заголовок `Cookie` из Request Headers.
5. Замените значение `HH_SESSION_COOKIE` в `.env`.
6. Перезапустите JobRadar и нажмите в боте «🔗 HH подключение» → «Проверить cookie HH».

## Отклики

По умолчанию включён безопасный режим «по кнопке» и тестовый режим. Бот показывает вакансию, готовит письмо и отправляет реальный отклик только после кнопки «🚀 Отправить отклик». В тестовом режиме реальный запрос в HH не выполняется: JobRadar пишет, что отклик был бы отправлен.

Полуавто включается в «⚙️ Настройки» → «🔁 Режим откликов» → «🟡 Полуавто». Бот складывает сильные вакансии в очередь и показывает их в «🧾 Мои отклики».

Авто включается только через «⚙️ Настройки» → «🔁 Режим откликов» → «🤖 Авто» → «✅ Я понимаю, включить автоотклики». Вакансии с тестами, внешним откликом, архивом, капчей и превышением лимитов не отправляются автоматически. При серьёзной ошибке автоотклики отключаются, если включён `auto_stop_on_error`.

Если `HH_SESSION_COOKIE` рабочий и HH подключен в боте, JobRadar открывает карточки вакансий как авторизованный пользователь. Когда HH показывает, что отклик уже был отправлен через приложение, сайт или другой клиент, бот записывает вакансию в журнал как внешний отклик и не присылает её повторно.

## Cron для Vercel или внешнего планировщика

Постоянный worker на Vercel невозможен, поэтому используйте защищённый endpoint:

```text
GET https://job-radar-ashen.vercel.app/api/cron/search?secret=HH_AUTO_WORKER_SECRET
```

Для текущего Python-запуска endpoint обслуживается встроенным aiohttp-сервером. На Vercel нужно направить `/api/cron/search` на Python-сервис или использовать внешний cron, который вызывает доступный URL запущенного JobRadar. Секрет должен храниться только в переменных окружения хостинга.

## Как работает scoring

Каждая вакансия получает:

- `score` от 0 до 100;
- статус `HOT`, `GOOD`, `MAYBE` или `REJECT`;
- карьерную пользу от 1 до 10;
- список плюсов и рисков.

Плюсы начисляются за удалёнку, IT-продукт, QA, баг-репорты, тикеты, Jira, Helpdesk, Service Desk, CRM, Bitrix24, amoCRM, API, SQL, Python, скрипты, автоматизацию, диагностику, логи и обучение.

Минусы начисляются за офис без удалёнки, высокий опыт, обязательное высшее образование, тяжёлые смены и клиентский сервис без технической части.

Жёсткие стоп-факторы сразу отклоняют вакансию: горячая линия, колл-центр, входящие или исходящие звонки, холодные звонки, телефонные продажи, обзвон клиентов, работа по скрипту.

## Как добавить новые поисковые запросы

Откройте файл `src/scoring.py` и добавьте фразы в список `SEARCH_QUERIES`.

Пример:

```python
SEARCH_QUERIES = [
    "junior qa",
    "техническая поддержка it",
    "ваш новый запрос",
]
```

После перезапуска бот начнёт использовать новые запросы.

## Где хранится база

SQLite-база создаётся по пути из `DATABASE_PATH`, по умолчанию:

```text
data/jobradar.db
```

В базе сохраняются найденные вакансии, оценки, причины, дата первого появления, дата последней проверки, дата отправки, избранное и ручные отклонения.

Если вакансия уже была отправлена, бот не пришлёт её повторно. Если нажать «Не подходит», вакансия больше не будет отправляться.

Внешние отклики с HH тоже сохраняются в `vacancy_log` со статусом `applied` и режимом `external`.

## Как в будущем добавить новые источники

SuperJob:
можно добавить отдельный клиент по аналогии с `src/hh_client.py`, если выбранный API-доступ остаётся бесплатным и стабильным.

Avito:
лучше не добавлять в MVP. Там выше риск блокировок и сложнее сделать бесплатную стабильную реализацию без браузерного парсинга.

Хабр Карьера:
можно добавить отдельный клиент, если доступ к вакансиям получится получать легально и стабильно без тяжёлого парсинга.

Для нового источника лучше сохранить общий поток:

```text
источник вакансий -> Vacancy -> calculate_score -> SQLite -> Telegram
```

Так scoring и защита от дублей останутся общими.

## Тестирование

Для запуска тестов установите `pytest` и запустите его из корня проекта:

```bash
pip install pytest
pytest
```
